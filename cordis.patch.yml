# dsh-cpa-status bundle patch: one insert adding the host row. The client half
# joins the browser graph automatically because package.json declares
# dsh.client and exports "./client".
- insert:
    - id: cpa-status
      name: dsh-cpa-status
